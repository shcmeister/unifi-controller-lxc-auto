#!/bin/bash

# Скрипт за автоматично създаване на LXC контейнер с UniFi Network Controller на Proxmox
# Използване:
#   ./install_unifi.sh [CTID] [WAIT_IP_TIMEOUT]
# CTID - ID на контейнера (по подразбиране автоматично следващото свободно)
# WAIT_IP_TIMEOUT - време в секунди за изчакване контейнерът да получи IP (по подразбиране 30)

# --- Конфигурация ---
CTID=${1:-$(pvesh get /cluster/nextid)}
WAIT_IP_TIMEOUT=${2:-30}

HOSTNAME="unifi-$CTID"
TEMPLATE="debian-12-standard_12.7-1_amd64.tar.zst"
MEMORY=2048
DISK_SIZE=10
CORES=2
MONGO_VERSION="7.0"

log_message() {
    echo -e "\033[1;34m--- $(date +'%Y-%m-%d %H:%M:%S') --- $1\033[0m"
}

if [[ $EUID -ne 0 ]]; then
    echo -e "\033[1;31mГрешка: Скриптът трябва да се стартира като root на Proxmox хост!\033[0m"
    exit 1
fi

if ! command -v pct >/dev/null 2>&1; then
    echo -e "\033[1;31mГрешка: 'pct' команда не е намерена. Изпълнявайте на Proxmox хост.\033[0m"
    exit 1
fi

log_message "Стартиране на инсталация на UniFi LXC контейнер с ID $CTID"

if pct status $CTID &>/dev/null; then
    echo -e "\033[1;31mГрешка: Контейнер с ID $CTID вече съществува. Изберете друг CTID.\033[0m"
    exit 1
fi

get_best_storage() {
    local pvesm_output
    pvesm_output=$(pvesm status 2>&1)
    if [ $? -ne 0 ]; then
        log_message "Грешка при 'pvesm status': $pvesm_output"
        return 1
    fi

    echo "$pvesm_output" | awk '
        NR==1 {
            for (i=1; i<=NF; i++) {
                if ($i == "Storage") NAME=i;
                if ($i == "Type") TYPE=i;
                if ($i == "Avail") AVAIL=i;
            }
            next
        }
        {
            name=$NAME; type=$TYPE; avail=$AVAIL
            if (name ~ /(iso|backup)/ || type !~ /(dir|lvmthin|zfspool)/ || name == "") next

            size=0; num=avail; unit=""
            if (avail ~ /[KMGTPEkmgtpe]$/) {
                unit=substr(avail,length(avail))
                num=substr(avail,1,length(avail)-1)
            }
            if (num ~ /^[0-9.]+$/) {
                if (unit ~ /[Kk]/) size=num/1024
                else if (unit ~ /[Mm]/) size=num
                else if (unit ~ /[Gg]/) size=num*1024
                else if (unit ~ /[Tt]/) size=num*1024*1024
                else size=num
            }
            if (size>0) print name, size
        }
    ' | sort -nrk2 | head -n1 | awk '{print $1}'
}

log_message "Намиране на storage с най-много свободно място..."
BEST_STORAGE=$(get_best_storage)
if [ -z "$BEST_STORAGE" ]; then
    echo -e "\033[1;31mГрешка: Не е намерен подходящ storage.\033[0m"
    exit 1
fi
log_message "Избран storage: $BEST_STORAGE"

log_message "Проверка за шаблон $TEMPLATE..."
if ! pveam list $BEST_STORAGE | grep -q "$TEMPLATE"; then
    log_message "Сваляне на шаблон $TEMPLATE..."
    pveam download $BEST_STORAGE $TEMPLATE || { echo -e "\033[1;31mГрешка: Неуспешно сваляне на шаблона.\033[0m"; exit 1; }
    log_message "Шаблонът е свален успешно."
else
    log_message "Шаблонът е наличен."
fi

ROOTPASS=$(openssl rand -base64 12)
log_message "Генерирана root парола за контейнера: $ROOTPASS"

log_message "Създаване на LXC контейнер $CTID ($HOSTNAME)..."
pct create $CTID "$BEST_STORAGE:vztmpl/$TEMPLATE" \
    -storage "$BEST_STORAGE" \
    -hostname "$HOSTNAME" \
    -memory "$MEMORY" \
    -cores "$CORES" \
    -rootfs "${BEST_STORAGE}:${DISK_SIZE}" \
    -net0 name=eth0,bridge=vmbr0,ip=dhcp \
    -unprivileged 1 \
    -onboot 1 \
    -password "$ROOTPASS" \
    -features nesting=1 || { echo -e "\033[1;31mГрешка при създаване на контейнера.\033[0m"; exit 1; }
log_message "Контейнерът е създаден."

log_message "Стартиране на контейнера $CTID..."
pct start $CTID || { echo -e "\033[1;31mГрешка при стартиране на контейнера.\033[0m"; exit 1; }
log_message "Контейнерът стартиран."

log_message "Изчакване на IP адрес (до $WAIT_IP_TIMEOUT секунди)..."
CONTAINER_IP=""
SLEEP_INTERVAL=5
MAX_ITER=$((WAIT_IP_TIMEOUT / SLEEP_INTERVAL))

for i in $(seq 1 $MAX_ITER); do
    sleep $SLEEP_INTERVAL
    CONTAINER_IP=$(pct exec $CTID -- hostname -I | awk '{print $1}')
    if [ -n "$CONTAINER_IP" ]; then
        break
    fi
done

if [ -z "$CONTAINER_IP" ]; then
    echo -e "\033[1;31mГрешка: Контейнерът не получи IP в зададеното време.\033[0m"
    exit 1
fi
log_message "Контейнерът получи IP: $CONTAINER_IP"

log_message "Инсталиране на UniFi Controller в контейнера..."
pct exec $CTID -- bash -c "
    set -e
    echo '--- $(date +'%Y-%m-%d %H:%M:%S') --- Инсталиране на UniFi Controller ---'

    apt-get update
    apt-get install -y curl gnupg apt-transport-https wget lsb-release ca-certificates

    wget -qO - https://dl.ui.com/unifi/unifi-repo.gpg | apt-key add -
    echo 'deb https://dl.ui.com/unifi/debian stable ubiquiti' > /etc/apt/sources.list.d/100-ubnt-unifi.list

    DISTRO_ID=\$(lsb_release -is | tr '[:upper:]' '[:lower:]')
    DISTRO_CODENAME=\$(lsb_release -cs)
    MONGO_REPO_TYPE='debian'
    if [ \"\$DISTRO_ID\" = 'ubuntu' ]; then
        MONGO_REPO_TYPE='ubuntu'
    fi

    wget -qO - https://www.mongodb.org/static/pgp/server-$MONGO_VERSION.asc | apt-key add -
    echo \"deb http://repo.mongodb.org/apt/\$MONGO_REPO_TYPE \$DISTRO_CODENAME/mongodb-org/$MONGO_VERSION main\" > /etc/apt/sources.list.d/mongodb-org-$MONGO_VERSION.list

    apt-get clean
    apt-get update

    apt-get install -y openjdk-17-jre-headless unifi

    apt-get clean && apt-get autoremove -y && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
"

if [ $? -ne 0 ]; then
    echo -e "\033[1;31mГрешка при инсталацията на UniFi Controller.\033[0m"
    exit 1
fi

echo -e "\n\033[1;32m✅ UniFi LXC контейнер $CTID е създаден и UniFi Controller инсталиран успешно!\033[0m"
echo -e "\033[1;36m🌐 Достъп до контролера: https://$CONTAINER_IP:8443\033[0m"
echo -e "\033[1;35m🔑 Root парола за SSH: \033[1;33m$ROOTPASS\033[0m"
echo -e "\033[1;33m⚠ Запишете паролата на сигурно място! След това създайте потребител със sudo и забранете root SSH.\033[0m"

exit 0
